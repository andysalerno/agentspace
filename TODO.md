A shared todo list for multiple agents to read and select tasks from:

- [ ] Use Monaco editor in the webui everywhere (skills editor, etc)
- [ ] Add dark mode to the webui
- [ ] navbar on the left in the webui should allow collapsing/minimizing
- [ ] add a kernel for codex cli (codex is already installed on this machine)
- [ ] add a rich-text cli client that has all the same abilities as the web UI, using a modern tui library