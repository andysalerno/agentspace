services:
  webui:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        AGENTSPACE_VERSION: ${AGENTSPACE_VERSION:-dev}
    env_file:
      - .env
    ports:
      - "8003:8003"
    extra_hosts:
      - "host.docker.internal:host-gateway"
