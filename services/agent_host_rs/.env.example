COPILOT_MODEL=claude-opus-4.6-1m
COPILOT_CONFIG_DIR=/root/.copilot
# Used as the default workdir passed to spawned kernel containers.
# KERNEL_WORKDIR=/tmp/agent-host
# Installation-scoped volume used by the built-in memory skill.
AGENT_HOST_SKILL_VOLUME_MEMORY__DATA=agentspace-memory-data
