[project]
name = "cli-channel"
version = "0.1.0"
description = "CLI channel proof of concept for AgentSpace"
requires-python = ">=3.13"
dependencies = ["httpx"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli_channel"]

[tool.pyright]
pythonVersion = "3.13"
typeCheckingMode = "strict"
